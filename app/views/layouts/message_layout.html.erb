<!DOCTYPE html>
<html>
  <head>
    <title>MessageBoardApplication</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
        <div class="navbar navbar-default">
            <div class="cotainer">
                <div class="navbar-header">
                    <%= link_to "MessageBoard", root_path, class: "navbar-brand" %>
                    <% if user_signed_in? %>
                        <%= link_to "New Message", new_message_path, class: "navbar-text navbar-link" %>
                    <% end %>
                    <ul class = "nav navbar-nav">
                        <% if !user_signed_in? %>
                            <li><%= link_to "Sign Up", new_user_registration_path %></li>
                        <% end %>
                        <% if user_signed_in? %>
                            <li class="menu"><%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "navbar-right" %></li>
                        <% else %>
                            <li><%= link_to "Log In", new_user_session_path %></li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </div>
  	    <p class="notice text-info"><%= notice %></p>
	    <p class="alert text-info"><%= alert %></p>
    <%= yield %>
  </body>
</html>
